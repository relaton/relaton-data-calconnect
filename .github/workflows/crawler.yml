# Auto-generated by Cimas: Do not edit it manually!
# See https://github.com/metanorma/cimas
name: Crawler

on:
  push:
    branches: [ master, main ]
    tags: [ v* ]
  pull_request:
  schedule:
  - cron: '0 14 * * *'
  workflow_dispatch:
    inputs:
      force:
        description: 'Force a fresh re-download (ignore cached ETag)'
        type: boolean
        required: false
        default: false

jobs:
  crawl:
    uses: relaton/support/.github/workflows/crawler.yml@main
    with:
      args: ${{ inputs.force && '--force' || '' }}
